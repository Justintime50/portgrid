**Variables to Use:**

- PR: <https://github.com/EasyPost/easypost-python/pull/384>
- JIRA_TICKET: EXP-811
- DOCS:
- SPECIAL_INSTRUCTIONS: "Ignore the name changes on the unrelated client tests, focus only on adding the new public interface and test"

---

**Objective**

Port the referenced PR to this project.

---

**Instructions**

- Port the PR exactly as it appears, adapting only for language or project conventions. Do not add or remove code unless required by the target language. Preserve comments as in the original PR.
- Our libraries follow similar conventions; only language syntax and nuances differ.
- Do not copy cassette files; these are auto-generated by tests. If (and only if) the original PR mocked the tests instead of using VCR, follow suit
- Run `clienv just test` to verify your changes. If tests fail, delete the generated cassette file for that test, fix the code, and rerun until all tests pass. For C#/.NET projects, use `clienv just unit-test`.
- Ensure linting passes with `just lint`. Use `just lint-fix` for auto-corrections.
- If additional information is available at $DOCS, consult it as needed.
- Follow existing project precedent and style for implementation details.
- For JavaScript/Node projects, add necessary TypeScript definitions. For .NET/C#, add parameter sets. For Go, add structs. For Java, add models/classes. Otherwise, ignore this instruction.

---

**Order of Operations**

1. Switch to the `master` branch.
2. Pull remote changes.
3. Implement your solution.
4. Test your solution.
5. Lint the code.
6. Create a draft PR with the $JIRA_TICKET as the branch name prefix. Use conventional commit messages. Populate the PR description with the same description as the referenced PR, or use a commit message if none exists.
7. Open the PR link in Chrome for review.

---

**Special Instructions**

Follow these if applicable:

$SPECIAL_INSTRUCTIONS
